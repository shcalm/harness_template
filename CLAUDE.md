# Claude Code 入口

本项目的 agent 治理规范、工作流、ID 体系、质量 Gate、状态词汇均定义在 `AGENTS.md`。

**立即动作**：开始任何工作前先读取 `AGENTS.md`（完整规范）与 `PROBLEM_SOLVING.md`（调试方法）。

> 本文件仅作 Claude Code 自动加载入口，不重复写规则；保持 `AGENTS.md` 为唯一治理事实源。
