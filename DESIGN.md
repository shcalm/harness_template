# DESIGN 模板：技术设计与恢复手册

<!-- DOC-CONTRACT: DESIGN.md
role: 技术设计与恢复事实源
status: permanent
read_policy: read before editing this file
required_sections: 环境基线, 恢复步骤, 架构概览, 接口与数据契约, 不变量, 技术决策记录, 可观测性, 失败模式与处理, 回滚与兼容, 已知技术债
ids: DEC-*, INV-*, OBS-*, BUG-*, DEBT-*, Contract-*
rules:
  - 设计结论必须能追溯到 REQ、AC 或 DEC
  - 环境、构建、运行命令必须可复制执行
  - 关键接口必须关联 REQ/AC
-->

本文件记录如何实现、为什么这样实现、如何恢复环境，以及遇到问题时如何定位。业务目标和验收标准写入 `PRD.md`。

## 1. 环境基线

| 项 | 要求 | 验证命令 |
| :--- | :--- | :--- |
| OS | `[发行版/版本]` | `uname -a` |
| 语言/运行时 | `[版本]` | `[命令]` |
| 包管理器 | `[版本]` | `[命令]` |
| 数据库/外部服务 | `[版本或说明]` | `[命令]` |
| 关键环境变量 | `[变量名和用途]` | `[如何检查是否存在]` |

## 2. 恢复步骤

从干净工作区恢复到可验证状态：

1. 安装依赖：`[命令]`
2. 环境检查：`./init.sh`
3. 构建：`[命令]`
4. 启动：`[命令]`
5. 最小健康检查：`[命令]`

成功标准：
* 所有命令退出码为 `0`。
* 健康检查输出 `[明确成功特征]`。
* 失败时日志路径为 `[日志路径]`。

## 3. 架构概览

```text
[外部调用方]
    -> [模块 A：职责]
    -> [模块 B：职责]
    -> [存储/外部服务：职责]
```

| 模块 | 职责 | 输入 | 输出 | 依赖 | 关联 REQ/AC |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `[模块名]` | `[单一职责]` | `[输入]` | `[输出]` | `[依赖]` | REQ-001 / AC-001 |

## 4. 接口与数据契约

| Contract-ID | 关联 REQ/AC | 调用方 | 接口/函数/消息 | 请求 | 响应 | 错误语义 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| API-001 | REQ-001 / AC-001 | `[调用方]` | `[接口]` | `[字段和约束]` | `[字段和约束]` | `[错误码/异常/重试规则]` |

## 5. 不变量

这些条件必须始终成立。违反不变量时，优先定位设计或状态管理问题。

| INV-ID | 不变量 | 保护位置 | 验证方式 |
| :--- | :--- | :--- | :--- |
| INV-001 | `[必须始终成立的条件]` | `[模块/测试/断言]` | `[命令或日志特征]` |

## 6. 技术决策记录

| DEC-ID | 关联 REQ/AC | 决策 | 替代方案 | 选择原因 | 代价/风险 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| DEC-001 | REQ-001 | `[选择方案]` | `[至少一个替代方案]` | `[为什么它更合适]` | `[副作用或维护成本]` | `[采用 / 废弃 / 待验证]` |

## 7. 可观测性

| Signal-ID | 类型 | 位置 | 说明 | 正常特征 | 异常特征 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| OBS-001 | `[日志 / 指标 / tracing / 产物]` | `[文件/模块/命令]` | `[观察什么]` | `[正常输出]` | `[异常输出]` |

## 8. 失败模式与处理

| BUG-ID | 现象 | 触发条件 | 关键日志 | 根因 | 处理方式 | 回归验证 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| BUG-001 | `[错误现象]` | `[如何触发]` | `[日志路径或摘要]` | `[确认后的根因]` | `[修复/规避方式]` | TEST-001 |

## 9. 回滚与兼容

| 场景 | 回滚方式 | 数据影响 | 兼容性风险 | 验证方式 |
| :--- | :--- | :--- | :--- | :--- |
| `[变更场景]` | `[如何回滚]` | `[是否影响数据]` | `[调用方/版本风险]` | `[命令或 AC-ID]` |

## 10. 已知技术债

| Debt-ID | 描述 | 影响 | 触发条件 | 后续任务 |
| :--- | :--- | :--- | :--- | :--- |
| DEBT-001 | `[技术债]` | `[影响范围]` | `[什么时候会变成问题]` | TASK-001 |

## 11. 最小填写示例

| DEC-ID | 关联 REQ/AC | 决策 | 替代方案 | 选择原因 | 代价/风险 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| DEC-001 | REQ-001 / AC-001 | 文件先保存到本地 `uploads/` 目录。 | 直接接入对象存储。 | MVP 阶段本地存储更容易验证完整上传链路。 | 多实例部署前必须替换存储层。 | 采用 |

| INV-ID | 不变量 | 保护位置 | 验证方式 |
| :--- | :--- | :--- | :--- |
| INV-001 | 下载链接只能指向已成功落盘的文件。 | 上传服务返回响应前 | `pytest tests/test_upload.py::test_link_points_to_existing_file -q` |
