# Claude Code Entry

This project's agent governance rules, workflow, ID scheme, quality gates, and status vocabulary live in `AGENTS.md`.

**Immediate action**: before any work, read `AGENTS.md` (full governance) and `PROBLEM_SOLVING.md` (debugging method).

> This file exists only as the Claude Code auto-load entry; it does not duplicate rules. `AGENTS.md` is the single source of truth.
